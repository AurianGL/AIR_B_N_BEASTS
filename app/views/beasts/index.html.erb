<!--   <div class="page-title mt-20">
    <h1>🐲 Rent (out) Fantastic Beasts. </h1>
  </div>

      <div class="container d-flex flex-wrap col-10">
        <% @beasts.each do |beast| %>

        <div class="card" style="width: 18rem;">

          <div class="card-img-top m-3 d-flex justify-content-center align-items-center" style="background-image: url('<%= cl_beast_path beast.photo %>')">

            <div class="mb-3" >
              <h5 class="card-title text-center"><%= beast.name %></h5>
              <p class="card-text"><%= beast.species %></p>

              <div class="card-body">
                <p class="card-text"><%= beast.price %>£</p>
                <p class="card-text"><%= beast.address %></p>
              </div>
            </div>
          </div>
          <div>
            <%= link_to 'Book Beast', beasts_path(@beast), class: "btn btn-primary mt-3 mb-5" %>
            <% end %>
          </div>

         </div>

       </div>

 -->
