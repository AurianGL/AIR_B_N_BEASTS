<div class="container d-flex flex-wrap">
  <div class="container-fluid col-4">
    <div class="m-3 d-flex justify-content-center align-items-center">
      <div class="show-card mb-3 col-10 justify-content-center align-items-center" >
        <div class="border-bottom dark mt-5">
          <h2><%= @beast.name %></h2>
          <p><i class="fas fa-dragon"></i> <%= @beast.species %></p>
          <p><strong>Owner is <%= @beast.user.first_name %></strong></p>
        </div>
          <div class="mt-3 mb-5"><p>Description: <%= @beast.description %></p>
            <p><i class="fas fa-pound-sign"></i> <%= @beast.price %>/day</p>
            <p><i class="fas fa-radiation-alt"></i> <%= @beast.dangerosity %>/5</p>
            <p><i class="fas fa-map-pin"></i> <%= @beast.address %></p>
          </div>
          <div class="mb-3 col-10">
            <%= link_to '📆 Check availabilities', new_beast_booking_path(@beast), class: "btn" %>
          </div>
      </div>

      <div class="container d-flex flex-wrap col-12">
        <div class="container-fluid">
          <div class="beast-card m-3 d-flex" style="background-image: linear-gradient(180deg, rgba(199,255,244,0.16541353383458646) 0%, rgba(2,0,36,0.5326505602240896) 49%, rgba(108,16,104,0.15449929971988796) 100%), url('<%= image_path "#{@beast.species}.jpg" %>')">
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


    <% if current_user == @beast.user %>
      <div class="d-flex">
        <% @bookings.each do |booking| %>
          <span><%= booking.user.first_name %></span>
          <% if booking.validate %>
            <span> 💚 </span>
            <% else %>
            <span><% link_to "🔴", beast_booking_path(@beast) %></span>
          <% end %>
        <% end %>
      </div>
    <% end %>





